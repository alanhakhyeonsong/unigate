# NN. 주제

> 한 줄 요약 — 이 문서를 덮고 나면 무엇을 말할 수 있어야 하는가
> 관련: Phase N · 커밋 `abc1234` · 코드 `gateway/src/.../Foo.kt`

## 1. 왜 필요했나

어떤 작업을 하다가 이 주제를 만났는지. 맥락 없는 개념 정리는 나중에 안 읽힌다.

## 2. 익숙한 방식과의 대조

| | Servlet / JPA / 블로킹 방식 | 여기서의 방식 | 왜 다른가 |
|---|---|---|---|
| | | | |

## 3. 동작 원리

(필요 시 Mermaid — 모든 노드 라벨은 `["..."]` 로 감쌀 것)

## 4. 직접 확인한 것

> ⚠️ **이 절의 유일한 불변 규칙** — 실제로 실행한 명령과 실제 출력만 쓴다.
> 예상 출력·추측 금지. 실행하지 않았으면 쓰지 않는다(`CLAUDE.md` §2.5).
> 비밀번호·토큰·세션 쿠키 원문은 마스킹한다.

실행한 명령/코드와 **실제 출력**을 남긴다. 추측이 아니라 관찰을 기록한다.

```bash
# 확인 명령
```

```
# 실제 출력
```

관찰: 

## 5. 함정 / 실패 모드

잘못 쓰면 어떤 증상으로 터지는가. 이 스택은 컴파일 에러가 아니라 **부하 시 런타임에** 터지는 게 대부분이다.
직접 겪었다면 **증상 → 원인 → 해결** 순으로 남긴다.

| 함정 | 증상 | 원인 | 해결 |
|---|---|---|---|
| | | | |

## 6. 남은 의문

검증 중 실제로 드러난 미해결 질문을 적는다. 다음 학습의 진입점이므로 가능하면 남긴다.
답이 나온 것과 아직 모르는 것을 나눠 쓰면 나중에 되짚기 쉽다.

### 이번에 답이 나온 것

- [x] 질문 → 답 (근거: §4 확인 N)

### 아직 모르는 것

- [ ] 질문 — 무엇을 하면 답이 나오는지까지 적는다

